import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#607d8b",
        },
        secondary: {
            main: '#fff',
        },
    }
})

export default theme;